import React, {PropTypes} from 'react';

export default React.createClass({
    render: function () {
        return (
            <section>
                <h1>About</h1>
            </section>
        );
    }
});